import styled from 'styled-components';

export const StyledContainer = styled.div`
  :hover {
    .btn {
      visibility: visible;
    }
  }
  .btn {
    visibility: hidden;
  }
`;

export default {};
