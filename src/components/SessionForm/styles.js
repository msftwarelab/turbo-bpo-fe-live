import styled from 'styled-components';

export const StyledDateContainer = styled.div`
  position: relative;

  .calendarIcon {
    position: absolute;
    top: 4px;
    right: 8px;
  }
`;

export default {};
