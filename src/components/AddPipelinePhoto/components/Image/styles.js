import styled from 'styled-components';

export const StyledContainer = styled.div`
  position: relative;
  .btn {
    position: absolute;
    right: 0;
    margin-right: 5px;
    margin-top: 5px;
  }
`;

export default {};
